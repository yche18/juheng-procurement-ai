services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: juheng
      POSTGRES_USER: juheng
      POSTGRES_PASSWORD: juheng_local
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U juheng -d juheng"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - juheng-postgres-data:/var/lib/postgresql/data

volumes:
  juheng-postgres-data:
